-- -- 1) DISEASES (ما يعتمد على أحد)
-- CREATE TABLE DISEASES (
--     DISEASE_ID NUMBER PRIMARY KEY,
--     DISEASE_NAME VARCHAR2(50) NOT NULL
-- );

-- -- 2) PATIENTS (يعتمد على DISEASES)
-- CREATE TABLE PATIENTS (
--     PATIENT_ID NUMBER PRIMARY KEY,
--     FULL_NAME VARCHAR2(100) NOT NULL,
--     DISEASE_ID NUMBER,
--     FOREIGN KEY (DISEASE_ID) REFERENCES DISEASES(DISEASE_ID)
-- );

-- -- 3) MEDICINES (يعتمد على DISEASES)
-- CREATE TABLE MEDICINES (
--     MEDICINE_ID NUMBER PRIMARY KEY,
--     MEDICINE_NAME VARCHAR2(100) NOT NULL,
--     DISEASE_ID NUMBER,
--     FOREIGN KEY (DISEASE_ID) REFERENCES DISEASES(DISEASE_ID)
-- );

-- -- 4) PATIENT_MEDICINES (يعتمد على الاثنين)
-- CREATE TABLE PATIENT_MEDICINES (
--     PM_ID NUMBER PRIMARY KEY,
--     PATIENT_ID NUMBER,
--     MEDICINE_ID NUMBER,
--     DOSAGE VARCHAR2(50),
--     START_DATE DATE,
--     FOREIGN KEY (PATIENT_ID) REFERENCES PATIENTS(PATIENT_ID),
--     FOREIGN KEY (MEDICINE_ID) REFERENCES MEDICINES(MEDICINE_ID)
-- );